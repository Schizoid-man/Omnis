Place your macOS SDK tarball here before running the macOS Docker build.

Expected filename: MacOSX14.0.sdk.tar.xz  (or whichever version you extracted)

See docs/macos-sdk-setup.md for extraction instructions.

This directory is in .gitignore — the SDK file will never be committed.
